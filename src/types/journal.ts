export interface JournalEntry {
  tradeId: string;

  reason: string;
  confluence: string;
  stopLoss: string;
  takeProfit: string;
  emotions: string;
  regrets: string;
  management: string;

  images: string[];
}